// Leftover file that should never live under .codex/** — imports the
// Claude Agent SDK, which is a Claude-Code-only mechanism.
const { query } = require('@anthropic-ai/claude-agent-sdk');
module.exports = { query };
