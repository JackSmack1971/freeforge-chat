---
phase: "01-accessible-settings-key-management"
plan: "01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - freeforge/index.html
  - freeforge/src/features/settings.js
  - tests/security/runtime-ui-features.test.mjs
  - tests/security/runtime-app.test.mjs
autonomous: true
requirements:
  - REQ-01
  - REQ-02
  - REQ-03
user_setup: []
must_haves:
  truths:
    - "Settings opens with the API key input focused for keyboard users."
    - "Validation, loading, clear confirmation, and success are announced without exposing the raw key."
    - "Closing Settings returns focus to the element that opened it."
  artifacts:
    - path: "freeforge/src/features/settings.js"
      provides: "Accessible settings modal behavior and key-management flow"
    - path: "freeforge/index.html"
      provides: "Modal semantics and accessible helper/status text"
    - path: "tests/security/runtime-ui-features.test.mjs"
      provides: "Keyboard and focus regression coverage for Settings"
    - path: "tests/security/runtime-app.test.mjs"
      provides: "Startup and wiring coverage for the Settings entry points"
  key_links:
    - from: "freeforge/src/features/settings.js"
      to: "freeforge/index.html"
      via: "focus the settings-new-key input and drive modal state/aria changes"
      pattern: "settings-new-key|settings-modal|aria-hidden"
    - from: "tests/security/runtime-ui-features.test.mjs"
      to: "freeforge/src/features/settings.js"
      via: "keyboard-only modal open/close and key update/clear assertions"
      pattern: "openSettings|closeSettings|updateKey|clearKey"

<objective>
Make the existing Settings modal task-focused and accessible for API key management.

Purpose: Reduce keyboard and screen-reader friction in the key update and clear flow without changing storage semantics.
Output: Accessible settings modal behavior and regression tests.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@freeforge/index.html
@freeforge/src/features/settings.js
@freeforge/src/ui/focus-trap.js
@tests/security/runtime-ui-features.test.mjs
@tests/security/runtime-app.test.mjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Focus the key field and tighten modal semantics</name>
  <files>freeforge/index.html, freeforge/src/features/settings.js</files>
  <read_first>freeforge/index.html, freeforge/src/features/settings.js, freeforge/src/ui/focus-trap.js</read_first>
  <action>Open Settings with the update-key input as the first focus target, keep the dialog semantics intact, and preserve the shared focus-trap behavior so close returns to the opener or a deterministic fallback without changing storage or clear-confirmation rules.</action>
  <verify>Keyboard-open Settings, confirm focus lands on #settings-new-key, and confirm close restores focus to the opener path.</verify>
  <acceptance_criteria>
    - `#settings-modal` keeps `role="dialog"` and `aria-modal="true"`.
    - Opening Settings focuses `#settings-new-key`, not `#close-settings-btn`.
    - Closing Settings restores focus to the element that opened it.
  </acceptance_criteria>
  <done>Settings opens on the key task and closes cleanly without breaking current storage behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Announce loading, error, and success states clearly</name>
  <files>freeforge/src/features/settings.js, freeforge/index.html</files>
  <read_first>freeforge/src/features/settings.js, freeforge/index.html</read_first>
  <action>Keep validation errors inline, surface loading and success state through the existing live-region hooks or equivalent accessible text, and ensure no status copy, toast, or error string ever includes the raw API key.</action>
  <verify>Run the key-update flow with valid and invalid keys and confirm visible text, announced text, and toast output never reveal the full key.</verify>
  <acceptance_criteria>
    - Invalid input keeps the modal open and leaves focus on `#settings-new-key`.
    - A successful update closes the modal and leaves the key masked in the UI.
    - No toast, status text, or error message contains the raw API key.
  </acceptance_criteria>
  <done>Users can recover from validation failures and complete updates without losing orientation.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the flow with browser-level tests</name>
  <files>tests/security/runtime-ui-features.test.mjs, tests/security/runtime-app.test.mjs</files>
  <read_first>tests/security/runtime-ui-features.test.mjs, tests/security/runtime-app.test.mjs, freeforge/src/features/settings.js</read_first>
  <action>Extend the existing browser-style tests to cover initial focus, keyboard trapping, update success, validation failure, clear confirmation, accessible status output, and focus restoration after close.</action>
  <verify>npm --prefix freeforge test</verify>
  <acceptance_criteria>
    - The settings tests fail before the focus change and pass after it.
    - The settings tests cover update success, validation failure, and clear confirmation.
    - The app wiring test still proves the settings entry points work from the chat shell.
  </acceptance_criteria>
  <done>The accessible settings path is pinned down by executable tests.</done>
</task>
</tasks>

<verification>
- [ ] npm --prefix freeforge test
- [ ] npx --yes @biomejs/biome@1.9.4 check freeforge/src tests/security
- [ ] Keyboard-only smoke test for open, update, clear, announce, and close
</verification>

<success_criteria>
- All tasks completed
- Keyboard users land on the key field when Settings opens
- Validation, loading, clear confirmation, and success are announced accessibly
- Closing Settings returns focus to the opener and storage behavior stays unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-accessible-settings-key-management/01-01-SUMMARY.md`
</output>
