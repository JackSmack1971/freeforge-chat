---
phase: 01-security-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - freeforge/src/markdown.js
autonomous: true
requirements:
  - SEC-04
  - SEC-07

must_haves:
  truths:
    - "marked.use() is called once at module load, not inside renderMd()"
    - "When DOMPurify is unavailable, renderMd() returns escaped text, never raw HTML"
    - "The catch branch continues to return esc(text).replace(/\\n/g, '<br>') unchanged"
  artifacts:
    - path: "freeforge/src/markdown.js"
      provides: "Safe markdown renderer with module-level marked initialization"
      contains: "marked.use({ breaks: true, gfm: true })"
  key_links:
    - from: "freeforge/src/markdown.js renderMd()"
      to: "marked.use() at module top level"
      via: "module initialization (not per-call)"
      pattern: "^marked\\.use\\("
    - from: "freeforge/src/markdown.js renderMd() ternary else"
      to: "esc(text).replace"
      via: "DOMPurify-unavailable fallback"
      pattern: "esc\\(text\\)\\.replace"
---

<objective>
Fix two security/correctness issues in freeforge/src/markdown.js: move the marked configuration
call from inside renderMd() to module-level initialization (SEC-07), and replace the raw-HTML
fallback in the DOMPurify-unavailable branch with escaped text (SEC-04).

Purpose: The ternary else `return raw` is an active XSS vector — if the DOMPurify CDN is
unreachable, LLM output containing script tags is injected verbatim into innerHTML. The per-call
marked.setOptions() is the deprecated v18 pattern. Both are one-line fixes that cost nothing
and close real vulnerabilities.

Output: freeforge/src/markdown.js where marked.use() fires once at import time and all fallback
paths return HTML-escaped text, never raw HTML.
</objective>

<execution_context>
@F:\ultimate-agentic-coding-swarm\.planning\phases\01-security-hardening\01-CONTEXT.md
@F:\ultimate-agentic-coding-swarm\.planning\phases\01-security-hardening\01-RESEARCH.md
@F:\ultimate-agentic-coding-swarm\.planning\phases\01-security-hardening\01-PATTERNS.md
</execution_context>

<context>
@F:\ultimate-agentic-coding-swarm\.planning\ROADMAP.md
@F:\ultimate-agentic-coding-swarm\.planning\REQUIREMENTS.md
</context>

## Phase Goal

**As a** portfolio visitor, **I want to** know that AI responses are always sanitized before they hit
the DOM, **so that** a CDN outage never turns the chat UI into an XSS execution surface.

<tasks>

<task type="auto">
  <name>Task 1: Fix markdown.js — module-init marked.use() and escape XSS fallback (SEC-04, SEC-07)</name>
  <files>freeforge/src/markdown.js</files>
  <read_first>
    - F:\ultimate-agentic-coding-swarm\freeforge\src\markdown.js — read the full file before any edits; it is 14 lines; identify line 6 (marked.setOptions call) and line 10 (ternary with raw HTML fallback)
  </read_first>
  <action>
    The file is 14 lines. Make exactly two edits:

    EDIT 1 — Move marked configuration to module level (SEC-07):
    Remove the line inside renderMd() that reads:
      marked.setOptions({ breaks: true, gfm: true });
    Add this line at module top-level, after the import statement and before the export function
    declaration:
      marked.use({ breaks: true, gfm: true });
    This must appear OUTSIDE of any function body. It executes once when the module is imported.
    Do not call marked.use() inside renderMd() or any other function — per marked.js documentation,
    marked.use() must not be called inside a function or loop.

    EDIT 2 — Fix the XSS vector in the ternary else branch (SEC-04):
    Locate the ternary on the line that reads:
      return typeof DOMPurify !== 'undefined' ? DOMPurify.sanitize(raw) : raw;
    The XSS vector is the ternary ELSE branch (the final `: raw`). Change only that part:
      Before: : raw
      After:  : esc(text).replace(/\n/g, '<br>')
    The resulting line is:
      return typeof DOMPurify !== 'undefined' ? DOMPurify.sanitize(raw) : esc(text).replace(/\n/g, '<br>');
    Do NOT touch the catch block — it already reads `return esc(text).replace(/\n/g, '<br>');` and is correct.
    Do NOT change the import statement — `esc` is already imported from './state.js'.

    The final file should match the target pattern from PATTERNS.md exactly:
    Line 1: import { esc } from './state.js';
    Line 2: (blank)
    Line 3: // Module-level initialization (SEC-07): call once at module load, not per-render.
    Line 4: // marked.use() must not be called inside a function or loop per marked.js docs.
    Line 5: marked.use({ breaks: true, gfm: true });
    Line 6: (blank)
    Line 7: export function renderMd(text) {
    Line 8:   if (!text) return '';
    Line 9:   try {
    Line 10:    const raw = marked.parse(text);
    Line 11:    // Sanitize LLM-generated HTML before injecting into the DOM.
    Line 12:    // SEC-04: fallback to escaped text (not raw HTML) when DOMPurify is unavailable.
    Line 13:    return typeof DOMPurify !== 'undefined' ? DOMPurify.sanitize(raw) : esc(text).replace(/\n/g, '<br>');
    Line 14:  } catch {
    Line 15:    return esc(text).replace(/\n/g, '<br>');
    Line 16:  }
    Line 17: }
  </action>
  <verify>
    <automated>grep -n "marked.use" /f/ultimate-agentic-coding-swarm/freeforge/src/markdown.js</automated>
    <automated>grep -n "marked.setOptions" /f/ultimate-agentic-coding-swarm/freeforge/src/markdown.js</automated>
    <automated>grep -n "return raw" /f/ultimate-agentic-coding-swarm/freeforge/src/markdown.js</automated>
    <automated>grep -n "esc(text)" /f/ultimate-agentic-coding-swarm/freeforge/src/markdown.js</automated>
  </verify>
  <acceptance_criteria>
    - freeforge/src/markdown.js contains "marked.use({ breaks: true, gfm: true });" at top level (before the export function line)
    - freeforge/src/markdown.js does NOT contain "marked.setOptions" anywhere
    - freeforge/src/markdown.js does NOT contain ": raw" as the ternary else value in the DOMPurify check
    - freeforge/src/markdown.js contains "esc(text).replace(/\n/g, '<br>')" in exactly two places: the ternary else branch and the catch block
    - The marked.use() call appears on a line with a smaller line number than the "export function renderMd" line (confirms top-level placement)
    - The import statement "import { esc } from './state.js';" is unchanged (esc was already imported; no new import needed)
  </acceptance_criteria>
  <done>markdown.js has marked.use() at module level only; all fallback paths return escaped text; no raw HTML injection possible on CDN failure</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM output → markdown.js → innerHTML | Untrusted text from OpenRouter passes through renderMd before DOM injection |
| jsDelivr CDN → DOMPurify | DOMPurify availability depends on CDN uptime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | markdown.js CDN-fail XSS vector (ternary else `return raw`) | mitigate | Replace `: raw` with `: esc(text).replace(/\n/g, '<br>')` — escapes all HTML entities before injection |
| T-02-02 | Tampering | marked.setOptions per-call (deprecated pattern, potential mis-configuration) | mitigate | Move to module-level marked.use() — configured once at import time, not per render call |
| T-02-03 | Tampering | XSS via marked parse output | mitigate | DOMPurify.sanitize(raw) on the happy path; esc() fallback on CDN failure |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — code-only edit |
</threat_model>

<verification>
After task completes:

1. `grep -n "marked.use" freeforge/src/markdown.js` — shows the call at a line number before the export function
2. `grep "marked.setOptions" freeforge/src/markdown.js` — returns empty (removed)
3. `grep ": raw" freeforge/src/markdown.js` — returns empty (XSS vector removed)
4. `grep "esc(text)" freeforge/src/markdown.js` — shows two occurrences (ternary else + catch)
5. Visual: Open app, send a message with a code block, table, and multi-line text — renders correctly (D-05)
</verification>

<success_criteria>
- markdown.js contains marked.use({ breaks: true, gfm: true }) exactly once, at module top level
- markdown.js does not contain marked.setOptions anywhere
- The ternary else branch returns esc(text).replace(/\n/g, 'br') not raw
- Both fallback paths (CDN unavailable + parse error) escape HTML before returning
</success_criteria>

## Artifacts This Phase Produces

| Symbol / Path | Type | Description |
|---------------|------|-------------|
| `renderMd(text)` | FUNCTION (modified) | Markdown renderer; safe on all code paths; marked configured at module init |

<output>
Create `.planning/phases/01-security-hardening/01-02-SUMMARY.md` when done.
</output>
