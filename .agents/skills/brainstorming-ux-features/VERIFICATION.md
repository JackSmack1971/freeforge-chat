# Verification

- Migrated from `.claude/skills/brainstorming-ux-features/` without modifying the source.
- Open Agent frontmatter validated with `quick_validate.py`.
- Windows smoke check: `python scripts/validate_feature_brief.py --help`.

## Remaining unknown

Interactive host behavior is not exercised by the standalone runner.
