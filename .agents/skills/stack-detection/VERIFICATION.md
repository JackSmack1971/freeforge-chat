# Verification

- Migrated from `.claude/skills/stack-detection/` without modifying the source.
- Removed unsupported source-runtime invocation metadata.
- Open Agent frontmatter validated with `quick_validate.py`.

## Remaining unknown

Desktop-client classification depends on the target repository and host environment.
