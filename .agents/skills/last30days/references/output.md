# last30days output

Load this for the report format and citation rules.

## General runs

- Start with the badge as the first line.
- Use `What I learned:` as the first prose line.
- Use bold lead-ins for the main points.
- Follow with `KEY PATTERNS from the research:` and a numbered list.
- Keep citations inline as `[name](url)`.
- Do not add a trailing `Sources:` block.

## Comparison runs

- Use the comparison title format instead of `What I learned:`.
- Keep the comparison headers required by the engine template.
- Preserve the same inline-link citation rule.

## Footer

- Pass through the engine footer verbatim.
- Do not invent or reformat the footer tree.
- Keep the final invitation line intact.
