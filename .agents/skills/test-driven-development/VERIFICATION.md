# Verification

- Shell helper was replaced with `scripts/run_tdd_cycle.py`.
- Target is cross-platform and uses `subprocess.run` with argument arrays.
- This repository has no project test runner, so the expected local result is a bounded no-runner error.
