# Verification

- Migrated from `.claude/skills/github-issue-to-pr/` without modifying the source.
- Removed unsupported source-runtime frontmatter while preserving workflow intent.
- Open Agent frontmatter validated with `quick_validate.py`.

## Remaining unknown

Issue/PR operations require a Git repository and authenticated GitHub tooling.
