# Verification

- Python AST check passed for all helper scripts.
- `list-skills.py --format json` completed against the configured GitHub source and returned 39 skills.
- Installation was not run because it mutates `$CODEX_HOME/skills` and requires an explicit user-selected source.
