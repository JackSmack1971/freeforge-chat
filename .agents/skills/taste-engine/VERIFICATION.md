# Verification

- Source-only metadata and legacy-runtime paths were not copied.
- Target has only supported `name`, `description`, and `compatibility` metadata.
- No profile was present in this repository, so no state mutation was attempted.
