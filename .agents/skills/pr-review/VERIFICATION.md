# Verification

- Migrated from `.claude/skills/pr-review/` without modifying the source.
- Rewrote hook guidance for optional Codex project hooks.
- Open Agent frontmatter validated with `quick_validate.py`.
- Windows smoke check: `python scripts/collect_pr_context.py --help`.

## Remaining unknown

PR collection requires a Git repository and provider credentials.
